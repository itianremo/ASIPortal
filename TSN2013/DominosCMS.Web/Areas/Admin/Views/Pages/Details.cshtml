@model DominosCMS.Models.Page
@{
    ViewBag.Title = "United OFOQ ::: " + Model.Title;

    ViewBag.Title = Model.Title;

}
@section header {
	<meta http-equiv="discription" content="@Model.Description" />
    <meta http-equiv="keywords" content="@Model.Keywords" />

    <style type="text/css">
        #left-pan {
            width: 70%;
            float: left;
        }
        #right-pan {
            float: left;
            margin-left: 10px;
            width: 28%;
        }
        
        .pagecontents ul li { list-style-type: disc ;}
        .pagecontents ol li { list-style-type: decimal;}
        .pagecontents li 
        {
             margin-left: 40px;
             
        }

    </style>
}

@{ Html.RenderPartial("_PageView", Model); }


